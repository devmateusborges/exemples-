datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model SysUser {
  id       String    @id @default(uuid())
  email    String @unique
  password String

  @@map("sys_user")
}
